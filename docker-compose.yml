services:
  db:
    image: mysql:8
    env_file: ./backend/.env
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

  backend:
    build: ./backend
    env_file: ./backend/.env
    depends_on:
      - db
    ports:
      - "3000:3000"

volumes:
  db_data:
